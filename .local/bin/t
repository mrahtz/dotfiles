#!/bin/bash

send_paths=""
for file in "$@"; do
    send_path=$(readlink -f "$file")
    send_paths="$send_paths '$send_path'"
done
if [[ $send_paths == "" ]]; then
    send_paths=$(pwd)
fi
echo "rtransfer $(hostname) $send_paths"
